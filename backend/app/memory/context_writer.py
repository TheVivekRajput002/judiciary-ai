"""context_writer.py — placeholder. Writing is handled inside remember_node."""
# The remember node writes directly to DB via SQLAlchemy.
# This module is reserved for any future extraction of write logic.
